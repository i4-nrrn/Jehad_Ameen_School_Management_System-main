# نظام إدارة المدرسة

نظام متكامل لإدارة المدرسة مُطور بلغة Java، يطبق مبادئ البرمجة الكائنية (OOP) بما في ذلك الوراثة، تعدد الأشكال، التجميع، المجموعات، تطوير الواجهات الرسومية، وتكامل قواعد البيانات.

---

## 📋 معلومات المشروع

**إعداد:** جهاد المنبهي  
**المجموعة:** الأولى  
**التخصص:** ذكاء أعمال  
**الجامعة:** جامعة العلوم والتكنولوجيا  
**العام الدراسي:** 2026

---

## 🎯 متطلبات المشروع المُحققة

✅ **الأصناف (Classes)** - أصناف متعددة مع التغليف المناسب  
✅ **الوراثة (Inheritance)** - Person ← Student, Teacher  
✅ **تعدد الأشكال (Polymorphism)** - الطرق المجردة وإعادة تعريف الطرق  
✅ **المجموعات (ArrayList)** - مستخدمة لإدارة الكورسات والطلاب  
✅ **التجميع (Aggregation)** - Course has-a Teacher, Student has-a Courses  
✅ **الواجهة الرسومية (GUI)** - واجهة مستخدم مبنية بـ Swing  
✅ **تكامل قاعدة البيانات** - الاتصال بقاعدة بيانات SQLite  
✅ **معالجة الاستثناءات** - معالجة شاملة للأخطاء  
✅ **كود نظيف** - كود منظم وموثق جيدًا  
✅ **التوثيق** - توثيق كامل للمشروع  

## 🏗️ معمارية النظام

### التسلسل الهرمي للأصناف
```
Person (الصنف الأساسي المجرد)
├── Student (يرث من Person)
└── Teacher (يرث من Person)

Course (علاقة التجميع مع Teacher)
SchoolDB (عمليات قاعدة البيانات)
SchoolApp (تطبيق الواجهة الرسومية)
```

### العلاقات الرئيسية
- **الوراثة**: `Student` و `Teacher` يرثان من `Person`
- **تعدد الأشكال**: طريقة `displayInfo()` مُطبقة بشكل مختلف في كل صنف فرعي
- **التجميع**: `Course` has-a `Teacher`, `Student` has-a list of `Course`
- **المجموعات**: `ArrayList<Course>` في Student, `ArrayList<Student>` في Course

## 📁 هيكل المشروع

```
نظام إدارة المدرسة/
├── src/
│   ├── Person.java          # الصنف الأساسي المجرد
│   ├── Student.java         # صنف الطالب مع الوراثة
│   ├── Teacher.java         # صنف المدرس مع الوراثة
│   ├── Course.java          # صنف الكورس مع التجميع
│   ├── SchoolDB.java        # عمليات قاعدة البيانات
│   └── SchoolApp.java       # تطبيق الواجهة الرسومية الرئيسي
├── lib/
│   └── sqlite-jdbc-3.x.x.jar  # مشغل SQLite JDBC
├── README.md                # هذا الملف
└── school.db               # قاعدة بيانات SQLite (تُنشأ تلقائياً)
```

## 🚀 كيفية التشغيل

### المتطلبات الأساسية
- Java Development Kit (JDK) 8 أو أعلى
- مشغل SQLite JDBC

### خطوات التثبيت

1. **تحميل مشغل SQLite JDBC**
   ```bash
   # تحميل من: https://github.com/xerial/sqlite-jdbc/releases
   # وضع sqlite-jdbc-3.x.x.jar في مجلد lib/
   ```

2. **ترجمة المشروع**
   ```bash
   cd "School Management System/src"
   javac -cp "../lib/sqlite-jdbc-3.x.x.jar" *.java
   ```

3. **تشغيل التطبيق**
   ```bash
   java -cp ".;../lib/sqlite-jdbc-3.x.x.jar" SchoolApp
   ```

## 💡 المميزات

### 🎓 إدارة الطلاب
- إضافة طلاب جدد بالرقم، الاسم، العمر، والمستوى الدراسي
- عرض جميع الطلاب في جدول منظم
- **تحديث بيانات الطلاب** (Edit)
- **حذف الطلاب** (Delete)
- **بحث بالاسم** للطلاب
- التحقق التلقائي من البيانات ومعالجة الأخطاء
- تخزين بيانات الطلاب في قاعدة بيانات SQLite

### 👨‍🏫 إدارة المدرسين
- إضافة مدرسين جدد بالرقم، الاسم، العمر، والمادة
- عرض جميع المدرسين في جدول منظم
- **تحديث بيانات المدرسين** (Edit)
- **حذف المدرسين** (Delete)
- **بحث بالاسم** للمدرسين
- تعيين المدرسين للكورسات
- استمرارية بيانات المدرسين في قاعدة البيانات

### 📚 إدارة الكورسات
- إنشاء كورسات بالرقم، العنوان، والسعة القصوى
- **تحديث بيانات الكورسات** (Edit)
- **حذف الكورسات** (Delete)
- **بحث بالعنوان** للكورسات
- تعيين مدرسين للكورسات (علاقة التجميع)
- عرض تفاصيل الكورسات بما في ذلك تعيينات المدرسين
- إدارة سعة الكورسات وحدود التسجيل

### 📝 تسجيل الطلاب
- تسجيل الطلاب في الكورسات المتاحة
- منع التسجيل المكرر
- تتبع حالة التسجيل في قاعدة البيانات
- اختيارات منسدلة سهلة الاستخدام
- **إلغاء تسجيل الطلاب** من الكورسات

### 🗄️ ميزات قاعدة البيانات
- قاعدة بيانات SQLite لاستمرارية البيانات
- إنشاء الجداول تلقائياً
- علاقات المفتاح الخارجي
- سلامة المعاملات ومعالجة الأخطاء
- **عمليات CRUD كاملة** (Create, Read, Update, Delete)

## 🔧 التنفيذ التقني

### مبادئ البرمجة الكائنية

#### 1. الوراثة (Inheritance)
```java
public abstract class Person {
    protected int id, age;
    protected String name;
    public abstract void displayInfo(); // تعدد الأشكال
}

public class Student extends Person {
    private int gradeLevel;
    private List<Course> courses; // المجموعات
    
    @Override
    public void displayInfo() { /* تطبيق خاص بالطالب */ }
}
```

#### 2. تعدد الأشكال (Polymorphism)
- طريقة `displayInfo()` المجردة في صنف Person
- تطبيقات مختلفة في أصناف Student و Teacher
- إعادة تعريف الطرق للسلوك المتخصص

#### 3. التجميع (Has-a Relationship)
```java
public class Course {
    private Teacher teacher; // Course HAS-A Teacher
    private List<Student> enrolledStudents; // Course HAS-A list of Students
}

public class Student {
    private List<Course> courses; // Student HAS-A list of Courses
}
```

#### 4. استخدام المجموعات
- `ArrayList<Course>` لتسجيل الطلاب في الكورسات
- `ArrayList<Student>` لتتبع تسجيل الكورسات
- `List<Teacher>` و `List<Student>` لعمليات قاعدة البيانات

### معالجة الاستثناءات
- أخطاء الاتصال بقاعدة البيانات
- انتهاكات قيود SQL
- أخطاء التحقق من المدخلات
- حماية من المؤشرات الفارغة
- رسائل خطأ سهلة للمستخدم

### مكونات الواجهة الرسومية
- **JTabbedPane**: واجهة منظمة مع علامات تبويب متعددة
- **JTable**: عرض البيانات مع DefaultTableModel
- **JComboBox**: الاختيارات المنسدلة للعلاقات
- **JTextField**: حقول إدخال المستخدم
- **JButton**: مشغلات الإجراءات مع مستمعي الأحداث

## 🎮 دليل الاستخدام

### إضافة طالب
1. اذهب إلى تبويب "Students"
2. املأ رقم الطالب، الاسم، العمر، والمستوى الدراسي
3. انقر "Add Student"
4. يظهر الطالب في الجدول أدناه

### إضافة مدرس
1. اذهب إلى تبويب "Teachers"
2. املأ رقم المدرس، الاسم، العمر، والمادة
3. انقر "Add Teacher"
4. يظهر المدرس في الجدول ويصبح متاحًا لتعيين الكورسات

### إنشاء كورس
1. اذهب إلى تبويب "Courses"
2. املأ رقم الكورس، العنوان، والسعة القصوى
3. اختر مدرسًا من القائمة المنسدلة (اختياري)
4. انقر "Add Course"
5. يظهر الكورس في الجدول

### تسجيل الطلاب
1. اذهب إلى تبويب "Enrollment"
2. اختر طالبًا من القائمة المنسدلة
3. اختر كورسًا من القائمة المنسدلة
4. انقر "Enroll Student"
5. يتم تسجيل التسجيل في قاعدة البيانات

### تعديل البيانات
1. في أي جدول، انقر على "Edit" في عمود Actions
2. يتم ملء الحقول تلقائياً بالبيانات الحالية
3. قم بتعديل البيانات المطلوبة
4. انقر زر الإضافة مرة أخرى لتحديث البيانات

### حذف البيانات
1. في أي جدول، انقر على "Delete" في عمود Actions
2. تأكيد الحذف في مربع الحوار
3. يتم حذف البيانات من قاعدة البيانات

## 🛠️ مخطط قاعدة البيانات

### الجداول التي تُنشأ تلقائياً

#### students
```sql
CREATE TABLE students (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    age INTEGER NOT NULL,
    grade_level INTEGER NOT NULL
);
```

#### teachers
```sql
CREATE TABLE teachers (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    age INTEGER NOT NULL,
    subject TEXT NOT NULL
);
```

#### courses
```sql
CREATE TABLE courses (
    course_id INTEGER PRIMARY KEY,
    title TEXT NOT NULL,
    teacher_id INTEGER,
    max_capacity INTEGER NOT NULL,
    FOREIGN KEY (teacher_id) REFERENCES teachers (id)
);
```

#### enrollments
```sql
CREATE TABLE enrollments (
    student_id INTEGER,
    course_id INTEGER,
    PRIMARY KEY (student_id, course_id),
    FOREIGN KEY (student_id) REFERENCES students (id),
    FOREIGN KEY (course_id) REFERENCES courses (course_id)
);
```

## 🔍 ميزات جودة الكود

### مبادئ الكود النظيف
- أسماء متغيرات وطرق ذات معنى
- مبدأ المسؤولية الواحدة
- تغليف مناسب مع getters/setters
- توثيق شامل
- تنسيق كود متسق

### معالجة الأخطاء
- كتل try-catch لعمليات قاعدة البيانات
- التحقق من صحة المدخلات لبيانات المستخدم
- رسائل خطأ لطيفة
- تنظيف الموارد (اتصالات قاعدة البيانات)

### أنماط التصميم
- **نمط MVC**: فصل الواجهة الرسومية، منطق الأعمال، والبيانات
- **نمط المصنع**: إنشاء الكائنات في طرق قاعدة البيانات
- **نمط المراقب**: معالجة أحداث الواجهة الرسومية

## 🚨 مشاكل شائعة وحلولها

### مشاكل الاتصال بقاعدة البيانات
- تأكد من وجود مشغل SQLite JDBC في classpath
- تحقق من أذونات الملفات لإنشاء قاعدة البيانات
- تحقق من توافق إصدار Java

### مشاكل عرض الواجهة الرسومية
- حدث مظهر وأسلوب Java Swing
- تحقق من توافق دقة الشاشة
- تأكد من استخدام مؤشر ترابط إرسال الأحداث بشكل صحيح

### أخطاء الترجمة
- تحقق من وجود جميع ملفات المصدر في مجلد src/
- تحقق من مسار مشغل JDBC في classpath
- تأكد من أن إصدار Java يدعم الميزات المستخدمة

## 🔮 تحسينات مستقبلية

- [ ] إدارة درجات الطلاب
- [ ] نظام جدولة الكورسات
- [ ] إنشاء التقارير (PDF/Excel)
- [ ] مصادقة المستخدمين والأدوار
- [ ] بحث وتصفية متقدم
- [ ] وظائف استيراد/تصدير البيانات
- [ ] إشعارات البريد الإلكتروني
- [ ] تتبع الحضور والانصراف

## 👨‍💻 ملاحظات التطوير

هذا المشروع يوضح مفاهيم برمجة Java المتقدمة:
- **إتقان OOP**: استخدام صحيح للوراثة، تعدد الأشكال، والتغليف
- **تطوير الواجهة الرسومية**: واجهة Swing احترافية مع معالجة الأحداث
- **تكامل قاعدة البيانات**: SQLite مع JDBC لاستمرارية البيانات
- **سلامة الاستثناءات**: معالجة شاملة للأخطاء في جميع أنحاء المشروع
- **معمارية نظيفة**: هيكل كود منظم وتوثيق جيد

---

## � معلومات المشروع

**إعداد:** جهاد المنبهي  
**المجموعة:** الأولى  
**التخصص:** ذكاء أعمال  
**الجامعة:** جامعة العلوم والتكنولوجيا  
**العام الدراسي:** 2026

---

## 📄 الترخيص

هذا المشروع مُطور لأغراض تعليمية لتوضيح مبادئ البرمجة الكائنية في Java.

---

**تُطور بـ ❤️ باستخدام Java, Swing, و SQLite**
